# Shared secret between Django backend and media server (REQUIRED)
# Generate one with: python -c "import secrets; print(secrets.token_urlsafe(32))"
MEDIA_SERVER_SECRET=

# Token validity in seconds (default: 900 = 15 minutes)
TOKEN_EXPIRY_SECONDS=900
